import IMeta from '../../interfaces/meta';

const meta: IMeta = {
  id: 'sl-homepage-redirect',
  description: 'Skip homepage and redirect straight to torrents page',
  title: 'Homepage redirect',
  defaultStatus: false,
};

export default meta;
